function rectangleArea(length, width) {
    let area;
    area = length * width;
    return area;
}
function perimeter(length, width) {
    let result;
    return result = 2 * (length + width);
}
console.log(rectangleArea(3, 4.5))
console.log(perimeter(3, 4.5))
